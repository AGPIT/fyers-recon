
# 8 items on 2026-08-04 23:46:38 UTC
- **Full v3 OpenAPI spec recovered** from `myapi.fyers.in/static/media/v3.fc0a0244d7d288c81e4f.yaml` (1.57 MB, 51 documented API paths) plus two supplemental specs: `v3.1.32a8eeba1fba866d1201.yaml` (SEBI retail-algo regulatory changes, effective Apr 2026) and `fia.9dcf545bc3f508df4db9.yaml` (new **FYERS MCP / Model Context Protocol** product).
- **Auth scheme confirmed live:** OAuth2 `authorization_code`; `appIdHash = SHA-256(app_id + ":" + app_secret)`; POST `/api/v3/validate-authcode {grant_type, appIdHash, code}`; access_token used as `Authorization: Bearer`. Live error fingerprinting: `-441` auth code required, `-442` invalid grant_type, `-16` invalid/expired token, `-501` invalid refresh token. `grant_type=refresh_token` accepted at `/api/v2/validate-refresh-token`; `authorization_code` rejected there (asymmetric v2/v3 handling).
- **New subdomains discovered** (from docs spec + probes): `trade.fyers.in` (200, OAuth redirect-uri host `/api-login/redirect-uri/index.html`), `socket.fyers.in` (`wss://socket.fyers.in/trade/v3`), `rtsocket-api.fyers.in` (`wss://rtsocket-api.fyers.in/versova`), `alerts.fyers.in`, `api-connect-docs.fyers.in` (branded order-placement docs + `fyers-lib.js`), `community.fyers.in`, `direct.fyers.in`, `learn.fyers.in`, `partners.fyers.in`.
- **New product surface:** FYERS MCP ships as a Mac `.pkg` / Windows `.exe` at `assets.fyers.in/mcp/macos/1.0.0/` that installs Node.js + Claude Desktop and performs its own FYERS OAuth login — client-side token storage / supply-chain surface (assets host is in scope).
- **marketdata-api-instaoptions.fyers.in** = Express service self-identifying as `{"service":"loom","version":"1.0.0"}` via `/health`; root 404s all paths probed.
- **api.fyers.in** (AWS ALB) exposed directly (no WAF), unlike api-t1/t2 (Cloudflare). Method enforcement confirmed: `OPTIONS /api/v3/order` -> 200; all others -> 500 with `{"message":"Invalid Request, please provide valid method"}`.
- API app management dashboard lives at `fyers.in/web/api-dashboard/user-apps` (Flutter web trading app on `fyers.in/web`). Docs portal SPA uses Redoc/Redocly and references `X-XSRF-TOKEN` CSRF handling.
- `workdrive.fyers.in`/`projects.fyers.in` redirect to Zoho SaaS (out-of-our-hands tenant; noted, deprioritized). `instantpayout.fyers.in` unreachable this run.

HIGH-IMPACT HYPOTHESIS IDENTIFIED (model: deepseek)
Review research_deepseek.md for details
## CVSS Candidates
- [H1 OAuth redirect_uri / auth-code interception] - Estimated CVSS 8.1 (if code->token leakage confirmed)
- [H1 open redirect on generate-authcode] - Estimated CVSS 6.1 (browser-only oracle)

# 2 items on 2026-08-05 01:23:50 UTC
- `api-t2.fyers.in/vagator/v2/*` — full endpoint set live (`get_user_id_v3`, `send_login_otp_v3`, `verify_otp_v2`, `verify_pin_v2`, `create_pin_v2`, `forgot_pin_v2`, `change_pin_v2`, `refresh_token_v2`, `verify_token_v2`, `totp`, `generate_qr`, `validate_qr`, `get_session_devices`, `guest_user/login_v2`, `guest_user/register`). Error shapes: `-1025 invalid request`, `-2 Missing request key`, `-1018 something went wrong`, `-1044 invalid input`, `verify_token_v2` returns DRF-style `{"detail":"Not authenticated"}`.
- `api.fyers.in/api/v2/*` (AWS ALB, no WAF) — same login verbs, form-POST validated, JSON-POST→500 invalid method.

HIGH-IMPACT HYPOTHESIS IDENTIFIED (model: deepseek)
Review research_deepseek.md for details

# 7 items on 2026-08-05 04:42:22 UTC
- **H1 redirect_uri oracle fully mapped (read-only).** For real clients `SOFG221ZX4-101` and `EFR7964223-101`, `GET /api/v3/generate-authcode` returned an identical 60,818-byte login page for three classes of `redirect_uri`: registered (`pledge.fyers.in/index.html`), external hostile (`evil.example.com/cb`), and foreign fyers.in subdomain (`direct.fyers.in/auth/redirect`). No server-side allowlist rejection at step-1; the unregistered URI is accepted. `V71C1UQU24-101` and subsequent calls hit CF **429/1015** rate limit — confirms a genuine unauthenticated public surface (no WAF app-layer filter, Cloudflare edge only). Invalid-format client_id → `302 trade.fyers.in/api-login/error/index.html?error_msg=invalid appType`, then 429.
- **No server-side URI reflection or echo in the login HTML.** Response body contains no reflection of the supplied `redirect_uri`, `client_id`, or `state`; the values are held client-side in JS. The login POST body itself includes `client_id`, `redirect_uri`, and `appType` (decrypted from string table). This means the *decision of where to deliver the auth_code is made client-side* off the URL param — consistent with step-1 not binding URI → code.
- **SSO bundle decrypted.** `login.fyers.in/new-sso/17.0/api_v3_login/login.min.js` (310 KB) uses a 1,257-element string-array obfuscation. Decoded backend routes: `api-t2.fyers.in/vagator/v2/*`, `api-t1.fyers.in/api/v3/{token, direct-login}`, `api.fyers.in/api/v2/*`, `tradingview/auth/*`. Confirmed string tokens: `refresh_token_v2`, `validate_refresh_token`, `REFRESH_TOKEN_VALIDITY`, `totp`, `get_user_id_v3`, `identifier_value`. Auth-flow fields: `#fy_client_id`, `client_id_flow`, `registered_client_id_and_pan`, `login_guest_user`, `register_guest_user`.
- **OTP-gate fingerprint proven.** `send_login_otp_v3` with the correct key `identifier_value` triggers CF **1015** ("error code: 1015" on the rendered page), whereas `mobile_number`/`client_id` keys return `-1025 invalid request`. This delineates a valid request-key vs invalid-key oracle AND confirms an active rate limiter on the OTP send path.
- **`api-g1.fyers.in/settings/*` new surface.** ~44 per-user settings endpoints (charts, key_ratio, key_metrics, screeners/favourites, one-click, scalper, order-window, qsgs, preferences, quick-trade). All return a **distinct auth fingerprint `{"code":-401,"message":"Unauthorized"}`** without a token — separate microservice, separate auth domain → candidate for cross-service authz gaps.
- **`api-i1.fyers.in/investment/*/fd/*` auth fingerprint.** Returns `{"error_code":40101,"Authorization token is required"}` — a third distinct auth error scheme (`-16`, `-401`, `40101`) across api-t1/t2, api-g1, api-i1.
- **Public `indus/v1/config` still live (H4 reproducible).** `https://api-d1-cdn.fyers.in/indus/v1/config` → 113,497 bytes, 73 endpoint blocks, 35 unique `*.fyers.in` hosts, `data_socket.cl_cug` (27 IDs: XS53027, XV15456, XT05527…), `analytics_exceptions.exceptional_clients` (14 IDs), `hsm_config.cug_mob` (9 IDs), plus feature/permission matrices (prime/basic `fundWithdrawal` limits), maintenance windows. The raw `api-d1.fyers.in` origin returns CF 1016 (origin-unreachable) — config is served only via the CDN wrapper.

HIGH-IMPACT HYPOTHESIS IDENTIFIED (model: deepseek)
Review research_deepseek.md for details
## CVSS Candidates
- [H1 OAuth code interception] — CVSS 8.1–9.0 (full ATO if code delivery confirmed)
- [H1 open-redirect / non-binding redirect_uri on generate-authcode] — CVSS 6.1 (confirmed step-1)

# 6 items on 2026-08-05 07:28:04 UTC
- **H1 step-A oracle matrix confirmed across 6 `redirect_uri` variants (read-only, live client `SOFG221ZX4-101`).** Registered (`pledge.fyers.in/index.html`), foreign-subdomain (`direct.fyers.in/auth/redirect`), external host (`evil.example.com/cb`), host-dot open-redirect (`pledge.fyers.inherit.example.com/cb`), and `?query#fragment` forms **all** render the identical **60,818-byte** login page. Byte-level and structural diffing of two fetches (same vs different URI) showed the only variance is per-request nonce/turnstile noise (~4 diff lines). **Conclusion: `generate-authcode` does NOT enforce an allowlist against the client's registered redirect_uri at step-1.** Only *presence* is validated.
- **Empty `redirect_uri` reveals the backend implementation detail.** Returns `302 → trade.fyers.in/api-login/error/index.html?error_msg=Key: 'AuthCodeRequest.RedirectURI' Error:Field validation for 'RedirectURI' failed on the 'required' tag`. This discloses: (a) the Go handler struct **`AuthCodeRequest.RedirectURI`**, (b) validation uses Go validator `required` tag (presence-only, not per-app registration). Strong PoC evidence for H1.
- **H3 field-walk oracle on `validate-refresh-token` proven (pre-auth, live).** Endpoint reachable without credentials and does progressive plaintext field validation: any `grant_type` other than `refresh_token` → `-442 "Please provide valid grant_type"`; `grant_type=refresh_token` advances to the next gate → `-501 "Please provide valid refresh token"`. Confirms a live refresh-token exchange surface with orderable validation branches.
- **SSO string-table cross-check** confirms backend token-family endpoints: `validate_refresh_token`, `validate_access_token`, `refresh_token_v2`, `REFRESH_TOKEN_VALIDITY`, `Bearer` header. `api/v3/validate-access-token` → 404 at that path (different route). `vagator/v2/validate_*` → 404.
- **Correction to H4:** the IDs leaked from `indus/v1/config` (`data_socket.cl_cug`, `analytics_exceptions.exceptional_clients`, `hsm_config.cug_mob`) are **7-char internal identifiers** (e.g. `XS53027`), NOT OAuth `client_id`s (real ones carry a `-101` appType suffix). Consequently they cannot be used to initiate an OAuth flow — H4 is downgraded to operational/internal identifier + feature-limit disclosure (still a publically reachable info-leak; reproducible 113,497-byte response, 73 endpoint blocks, 35 unique `*.fyers.in` hosts).
- **Auth-domain partitioning reconfirmed** (distinct, non-interoperable no-token fingerprints): `-16`/`-15` (api-t1/t2 trade+login core), `-401` (api-g1 settings), `40101` (api-i1 fixed-income), DRF `{"detail":...}` (verify_token_v2, guest_user/register). No token exists to bridge them → token-scoping remains design-only (a real api-t1 token is required to test api-g1 acceptance).

HIGH-IMPACT HYPOTHESIS IDENTIFIED (model: deepseek)
Review research_deepseek.md for details
**Impact:** Open redirect to arbitrary/external/cross-host URLs from a trusted FYERS domain (CVSS 6.1); if post-auth code delivery honors the untrusted URI client-side with no registry recheck (per the decoded `login.min.js` flow), full trading-account takeover via code interception (CVSS 8.1–9.0).
**Impact:** If refresh tokens remain honored post-deprecation or PIN/TOTP precedence is weak, token-rotation gaps could enable session/account takeover (CVSS 7.5–9.0).
## CVSS Candidates

# 5 items on 2026-08-05 10:14:51 UTC
- **H1 static deliverable complete: post-auth redirect destination is driven by client-side URL parameters.** Full deobfuscation of the SSO bundle (`login.min.js`, 310 KB, string-array obfuscation, 2 rotated arrays) shows the authenticated redirect handler reads `redirect_uri` **and a second alias `cb`** straight from the current page's query string and sets `location.href` to them (`if(_0x1b1ef9['redirect_uri']){location.href=…} else if(_0x1b1ef9['cb']){location.href=…}`), with only a 5-value reserved-`cb` denylist (`includes` check). The QR/WebSocket login success path pushes `_FYERS|<cookie>|_deviceId` (pipe-delimited) and then navigates to the URL-param destination. No server-side registry lookup on the destination is present anywhere in the flow. Combined with the confirmed step-1 oracle (Go `required`-tag, presence-only validation of `AuthCodeRequest.RedirectURI`), this closes the H1 PoC evidence chain for an **open-redirect-on-login primitive**; whether the code/cookie artifact is appended to the attacker URI can only be confirmed with authenticated testing (out of scope).
- **H3 complete field-walk on `validate-refresh-token` (pre-auth, live).** Progressive, orderable validation errors establish the exact request schema: `grant_type`≠`refresh_token`→`-442`; empty `refresh_token`→`-501`; missing `appIdHash`→`-371 "Please provide sha256 hash of appId and app secret"` (discloses the hashing scheme, matching the public `SHA256(appId:appSecret)`); missing `pin`→`-502 "Please provide your valid pin"`; all-present dummy values→`-501` (token validity gate last). Order: `grant_type → refresh_token → appIdHash → pin → token validity`. This is a clean pre-auth enumeration oracle for the refresh-token exchange and confirms v3 requires `pin` at the *validate* layer.
- **`/api/v3/token` refresh grant is fully opaque (`-16` uniform).** Unlike `validate-refresh-token`, the real token endpoint returns `-16` for every refresh grant permutation (missing/format-fine fields alike) — no field oracle. The two endpoints are separate services with divergent error behavior; any pin-requirement divergence between *validate* and *token* must be tested with a valid token (documented, not executed).
- **api-a1 splits into two independent auth domains.** `funds/v1/api/*` (40+ bank/mandate/withdrawal endpoints) → `-21 "Authorization token required"`; `manage/account/*` (90+ endpoints) → `-16`. Distinct from api-g1 (`-401`), api-i1 (`40101`), api-t2/baskets (`401 "missing authorization header"` plain text), api-t2/indri (`-16`), and DRF (`{"detail":"Not authenticated"}`). This yields ≥6 auth fingerprints across the estate — the core input to the H5 cross-service authz hypothesis.
- **Public config re-confirmed live** (113,497 B, 73 endpoint blocks): `api-a1.fyers.in` alone is referenced by **212 URLs** in the endpoint map, including sensitive objects (esign/initiate, selfie-validation, freeze/account, close-account/transfer-details, add_bank/penny_drop) — a stable recon artifact for further object-ID analysis.

HIGH-IMPACT HYPOTHESIS IDENTIFIED (model: deepseek)
Review research_deepseek.md for details
**Impact:** Confirmed open redirect from a trusted FYERS login host (CVSS 6.1). If the auth code is URL-delivered on that client-side redirect, the attacker (who controls `code_challenge`/verifier in the phished URL) can redeem it — PKCE offers no protection because the attacker controls both ends → full ATO (CVSS 8.1–9.0). Requires authenticated confirmation of the delivered artifact.
**Impact:** If `/token` refresh exchange omits the pin check that `validate-refresh-token` enforces, a leaked refresh_token alone mints access tokens (CVSS 7.5–9.0).
## CVSS Candidates

# RECON round 2026-08-05 19:xx UTC
- **New in-scope host `mcp.fyers.in` (FYERS MCP, Model Context Protocol).** `GET /mcp` → SSE `text/event-stream` (200, holds open, no auth error). Unauthenticated JSON-RPC `initialize` POST returns `{"serverInfo":{"name":"Fyers Trading MCP Server","version":"1.0.0"},"capabilities":{"resources":{},"tools":{"listChanged":true}}}`, `protocolVersion 2025-03-26`. `tools/list` (no session) → `Invalid session ID`; SSE sessions require `Authorization: Bearer ${FIA_TOKEN}` (per public fia.yaml spec). Root/health/mcp.json/.well-known/mcp → 404 text/plain 19B. Distinct auth domain: FIA_TOKEN bearer vs OAuth access_token. Source: `myapi.fyers.in/static/media/fia.9dcf545bc3f508df4db9.yaml`.
- **New in-scope host `automate-chn.fyers.in`** (Cloudflare, 104.18.4.135). `/automate/backtest/status` → `-16` trade-core; `/automate/backtest/` and root → 404. Mirrors api-t2 automate backend on a second hostname — token-scoping candidate vs api-t2.
- **New auth fingerprint #7:** `api-t2.fyers.in/quantainer/v1/charts/{futures,atm-iv,multi-strike-iv,strategy,...}` → `{"code":-401,"message":"Please provide valid token"}` — distinct message from api-g1 `-401 Unauthorized` (separate microservice/auth domain).
- **Full `api-t2.fyers.in/automate/*` inventory recovered from public config** (correct base, not `/automate/v1/`): `backtest/{history,preflight,start}`, `copy?strategy_id=`, `details?strategy_id=`, `draft?strategy_id=`, `explore`, `export`, `get-configuration?task_variant=MultilegOptions&task_id=PlaceOrder`, `get-nodes`, `my/{activity,automation,drafts,pnl,has-automations,strategy/*}`, `schedule/{create,cancel}`, `strategy/pnl?strategy_id=`, `workflow/{stop,multi-stop}`, `workflows`, `tagpositionbook`, `osb/draft?strategy_id=`. All `-16` auth-gated (confirmed live on samples).
- **`data.fyers.in/dev-fyers/savechart/1.2/charts`** → `500 "Invalid Request, please provide valid method"` — session-gated form-POST surface (method-enforcement pattern), object-keyed (saved charts) IDOR candidate behind auth.
- **api-a1 auth split finalized:** `funds/v1/api/{send_otp,verify_otp,add_funds_status,transaction_download_key,...}` all `-21`; `manage/account/{send_otp,verify_otp}` and `marina/v1/mtf/{send_otp,verify_otp}` all `-16`. No public OTP oracle on api-a1.
- **api-i1 fd/* uniformly `40101`** (`plans`, `filters`, `overview`). **screeners.fyers.in/koshi/v2/* now `-16`** (an earlier run saw `/config` 200 — surface since locked).
- **Auth-model documentation (live):** v3.1 SEBI spec (`myapi.fyers.in/static/media/v3.1.32a8eeba1fba866d1201.yaml`) confirms refresh-token flow discontinued, daily 2FA mandatory, static-IP whitelist for order placement, single transactional App ID per user, market orders auto-converted to MPP, no AMO. Contact `api-support@fyers.in`.
- **Config drift:** `indus/v1/config` grew 113,497 → 114,456 B; current dump references 870 `*.fyers.in` URLs.

# 8 items on 2026-08-05 19:18:03 UTC
- **New in-scope host `mcp.fyers.in` — FYERS MCP (Model Context Protocol) SSE service.** `GET /mcp` → `text/event-stream` (200, holds open). An **unauthenticated JSON-RPC `initialize` POST returns server identity and capability schema** with no token: `Fyers Trading MCP Server v1.0.0`, `protocolVersion 2025-03-26`, capabilities `resources` + `tools(listChanged)`. `tools/list` without an SSE session → `Invalid session ID`; sessions require `Authorization: Bearer ${FIA_TOKEN}` (per `myapi.fyers.in/static/media/fia.*.yaml`). Root, `/health`, `/mcp.json`, `/.well-known/mcp` → 404 (19 B text). This is a **seventh auth domain** (FIA_TOKEN bearer) vs OAuth `access_token` — a fresh token-scoping/cross-service candidate.
- **New in-scope host `automate-chn.fyers.in`** (Cloudflare 104.18.4.135): `/automate/backtest/status` → `-16` trade-core; `/automate/backtest/` and root → 404. Second hostname serving the same automate backend as api-t2 — cross-host token-scoping candidate.
- **New auth fingerprint #7 confirmed:** `api-t2.fyers.in/quantainer/v1/charts/*` (`futures`, `atm-iv`, `multi-strike-iv`, `strategy`, …) → `{"code":-401,"message":"Please provide valid token"}` — message distinct from api-g1 `-401 Unauthorized`, i.e. another independent microservice/authz domain.
- **Full `api-t2/automate/*` + `osb/*` inventory recovered from public config** (correct base, not `/v1/`): `backtest/{history,preflight,start}`, `copy|details|draft?strategy_id=`, `explore`, `export`, `get-configuration?task_variant=MultilegOptions&task_id=PlaceOrder`, `get-nodes`, `my/*`, `schedule/*`, `strategy/pnl?strategy_id=`, `workflow/{stop,multi-stop}`, `workflows`, `tagpositionbook`, `osb/draft`. Sampled endpoints all `-16` auth-gated — object-ID (`strategy_id`) surfaces behind auth.
- **api-a1 auth split finalized:** `funds/v1/api/{send_otp,verify_otp,add_funds_status?order_id=,transaction_download_key}` → `-21`; `manage/account/{send_otp,verify_otp}` and `marina/v1/mtf/{send,verify}_otp` → `-16`. **No public OTP oracle exists on api-a1.**
- **api-i1 `fd/{plans,filters,overview}` uniformly `40101`; screeners.fyers.in/koshi/v2/* now `-16`** (an earlier run observed `/config` 200 — that surface has since been locked).
- **Auth-model intel (live docs, `myapi.fyers.in`):** v3.1 SEBI spec confirms refresh-token discontinuation, mandatory daily 2FA, static-IP whitelisting for order placement, single transactional App ID per user, MPP-forced market orders, no AMO; official API contact **`api-support@fyers.in`** (usable as disclosure contact). `fyers.in/{security,privacy,responsible-disclosure}` all 404.
- **Config drift:** `indus/v1/config` grew 113,497 → 114,456 B; current dump enumerates **870 `*.fyers.in` URLs**.

HIGH-IMPACT HYPOTHESIS IDENTIFIED (model: deepseek)
Review research_deepseek.md for details
## CVSS Candidates
- [H8 MCP unauthenticated identity/capability disclosure] — CVSS 5.3
- [H8 MCP FIA_TOKEN cross-domain scoping / tool ATO] — CVSS 6.5–8.0 (conditional)

HIGH-IMPACT HYPOTHESIS IDENTIFIED (model: deepseek)
Review research_deepseek.md for details

# SURFACE round 2026-08-05 20:xx UTC
- **H8 boundary oracle finalized — `mcp.fyers.in` auth is enforced ONLY at POST dispatch, not on the SSE stream.** Full JSON-RPC walk (no token): `initialize` → 200 identity+capabilities (only unauth method); `notifications/initialized`, `tools/list`, `tools/call`, `resources/list`, `resources/templates/list`, `prompts/list`, `ping` → all `Invalid session ID`. `GET /mcp` returns 200 `text/event-stream` holding open with a bogus `Authorization: Bearer` (no sessionId event, no error, no auth check) — the stream open is unauthenticated, session gate lives at request dispatch. `.well-known/oauth-*`, `/oauth`, `/mcp/oauth` all 404 → OAuth metadata not exposed server-side (native flow via client).
- **MCP macOS `.pkg` statically dissected (assets.fyers.in/mcp/macos/1.0.0/FYERS-Claude-MCP-Installer-v1.0.0.pkg, 22,175 B xar).** Contains: `Distribution`/`PackageInfo` (`com.fyers.claudeinstaller`, postinstall), `welcome/license.html`, and a 15,872 B gzip `postinstall` bundle with 3 shell scripts. Behaviour: downloads Node.js v20.18.0 and Claude.dmg, then writes `~/Library/Application Support/Claude/claude_desktop_config.json` with `{"mcpServers":{"FYERS-MCP":{"command":"npx","args":["mcp-remote","https://mcp.fyers.in/mcp"]}}}`. **No token/scopes embedded** — auth is native MCP OAuth (`mcp-remote`), confirming H8 relies purely on the FIA_TOKEN bearer at the SSE endpoint. Windows .exe is 77,235,112 B (not pulled; noted for later static analysis if needed).
- **api-i1 split into four independent auth domains (fingerprints #9–#11).** `investment/*/fd/*` → `40101 "Authorization token is required"`; `mf/data/v2/*` gated set → `-101 "Authorization token is required"`; `mf/txn/v1/*` → `-101 "Authorisation token is missing."` (message variant); `investment/tapi/v1/ipo/v2/*` → `-100 "Authorisation token required."`. Four distinct error schemes under one hostname ⇒ per-service authz domains.
- **NEW: raw Microsoft SQL Server error leak (unauthenticated) on `api-i1.fyers.in/mf/data/v2/scheme/details/chart`.** With valid `period` (1M/1Y/3M/5Y; `1D` is invalid) returns `{"Number":1103,"SQLState":[52,50,48,48,48],"Message":"Incorrect table name ''"}` — SQLState decodes to "42000". Backend is SQL Server; the chart table name is interpolated server-side (empty for unknown params), and the raw DB error is reflected with no wrapper and **no auth**. isin/plan_id/symbol params accepted but do not set the table name → table name derived server-side. Error-based oracle + stack disclosure on a public endpoint (H10, CVSS 4.3 baseline; SQLi conditional on table-name provenance).
- **Public `mf/data/v2` data tier enumerated (api-i1).** No-auth 200: `home` (plan_id/ISIN/NAV/AUM/expense_ratio/ratings of top funds), `assortments` (category-wise index values), `collateral` (funds eligible as margin incl. haircut), `explore/filters` (rating/risk/AMC enum), `scheme/details/about?isin=` (fund objective), `scheme/details/holding-allocation?isin=` (equity/debt split, sector allocation), `scheme/details/chart` (SQL leak). Gated `-101`: `scheme/details/overview`, `get_order_window`, `redeem-ow`, `sip_schedule_dates`. Auth-gate ordering is inconsistent per-endpoint (validation-before-auth on `about`; auth-before-params on `overview`).
- **NEW: `api.fyers.in/fy/cdsl/dev/*` pre-auth input-validation surface.** `{cdslEdis,edis,pledge}/{index,details,tpin,userdata,setup,status,reqdetails}` respond with input errors ("Invalid request method" on GET, "Invalid Input Format" on POST `{}`) — **no auth fingerprint at all** (no `-16`/`-401`/`-21`). The non-`dev` twins (`/fy/cdsl/cdslEdis/index` etc.) return the standard session-gate `500 "Invalid Request, please provide valid method"`. ⇒ a dev-stage EDIS/TPIN (electronic-delivery-instruction / pledge) service on the prod `api.fyers.in` host validates input before any observable auth; EDIS + TPIN = securities-transfer primitive (H11, behind input schema unknown; PoC = request-schema field-walk read-only).
- **`vagator/v2/get_tradecast_token` is a pre-auth GET oracle** (`{"s":"error","code":-1044,"message":"invalid input"}` for any/no param; auth header ignored) — tradecast websocket-token endpoint, param schema not yet recovered from any JS bundle (absent from trade.js/fyers_common_modules.js).
- **`watson-obs/v1/telemetry`** (api-t2) POST → `-99 "unauthorised access token"` — auth fingerprint #8; GET → 404.
- **Config-drift deltas vs prior dump:** new quantainer surface `quantainer/v1/oi_stats/{change,drops,historical_pcr_max_pain,interpretation,max_pain,multi_strike,spurts,time_vs_oi,trending}` all `-401 "Please provide valid token"`; new `alpha/v1/client` → 404; `osb/my` and `indus/history/greeks/real-time` → `-16`; `automate-chn.fyers.in/automate/backtest/{cancel,result,status,trade-book-export,trade-book}` (all `-16`). Host count now 36 (35 + no change), URL count 870.
- **`api-t1-cdn.fyers.in/v1/screeners/*`** (SWOT/about/similar + ~30 candlestick patterns) → 503 Cloudflare challenge — screeners surface moved behind a bot gate; not publicly readable this run.
- **Public `config.fyers.in/config/prime_banner*.json`** → 200 marketing-banner image URLs (assets host); `public.fyers.in/{messages/public.json, mpp_details/mpp_config.json, app/smartSearchText.json}` → 200 public content; `public.fyers.in/sym_details/` is S3 (NoSuchKey on dir) with 30 symbol-master files (CSV/JSON/gz) — standard public symbol data.

HIGH-IMPACT HYPOTHESIS IDENTIFIED (model: deepseek)
Review research_deepseek.md for details
## CVSS Candidates
- [H10 raw SQL Server error disclosure on mf/data/v2/scheme/details/chart] — CVSS 4.3 (unauthenticated stack/error oracle; SQLi conditional, behind schema knowledge)
- [H11 api.fyers.in cdsl/dev EDIS/TPIN pre-auth input-validation surface] — CVSS 5.3–7.5 (validation-before-auth; securities-transfer primitive conditional on param schema + missing auth)
- [H8 MCP unauthenticated identity/capability disclosure] — CVSS 5.3
- [H8 MCP FIA_TOKEN cross-domain scoping / tool ATO] — CVSS 6.5–8.0 (conditional)

# 7 items on 2026-08-05 21:25:51 UTC
- **Raw SQL Server error leak (unauthenticated)** on `api-i1.fyers.in/mf/data/v2/scheme/details/chart`: valid `period` values (1M/1Y/3M/5Y) return `{"Number":1103,"SQLState":[52,50,48,48,48],"Message":"Incorrect table name ''"}` (SQLState = "42000"). Backend = MS SQL Server; chart table name interpolated server-side, raw DB error reflected with no wrapper and no auth. New H10.
- **api.fyers.in `fy/cdsl/dev/*` pre-auth input-validation surface** (H11): `{cdslEdis,edis,pledge}/{index,details,tpin,userdata,setup,status}` return input errors ("Invalid request method"/"Invalid Input Format") with **no auth fingerprint**, while non-dev twins return the session-gate `500`. Dev-stage EDIS/TPIN (securities-transfer) service validates before auth on prod.
- **MCP .pkg dissected**: 22 KB xar → postinstall writes Claude config `npx mcp-remote https://mcp.fyers.in/mcp`; no embedded token/scopes. H8 relies purely on FIA_TOKEN at the SSE endpoint; auth enforced only at POST dispatch (`initialize` is the sole unauth method).
- **api-i1 = 4 auth domains**: `40101` (fd), `-101 "Authorization token is required"` (mf/data gated), `-101 "Authorisation token is missing."` (mf/txn), `-100 "Authorisation token required."` (tapi IPO).
- **Public mf/data tier**: home/assortments/collateral/explore-filters/about/holding-allocation public; overview/get_order_window/redeem-ow/sip_schedule_dates gated.
- **Auth fingerprint #8**: `watson-obs/v1/telemetry` POST → `-99 "unauthorised access token"`.
- Config drift: +`quantainer/v1/oi_stats/*` (-401), `osb/my`/`indus/history/greeks/real-time` (-16), automate-chn backtest set (-16); `api-t1-cdn screeners` now Cloudflare-503.

HIGH-IMPACT HYPOTHESIS IDENTIFIED (model: deepseek)
Review research_deepseek.md for details
 - [H8 MCP FIA_TOKEN cross-domain scoping / tool ATO] — CVSS 6.5–8.0 (conditional)
+- **NEW: SQL error leak (unauthenticated) on `api-i1.fyers.in/mf/data/v2/scheme/details/chart`.** With valid `period` (1M/3M/6M/1Y/3Y/5Y; 1D/1W invalid) returns `{"Number":1103,"SQLState":[52,50,48,48,48],"Message":"Incorrect table name ''"}` (SQLState="42000"). Backend = MS SQL Server; no wrapper and no auth.
+
+# SURFACE round 2026-08-05 22:48 UTC
+- **H10 closed on chart table-name provenance.** `scheme_code` is the table selector: `scheme_code=47941&period=1Y` returns full public NAV history (`nav_history[]` timestamps+NAV); `scheme_code` is **parameterized** (strings/non-exist codes → empty `nav_history[]`, no injection). The raw SQL-1103 error only surfaces on the `scheme_code`-absent / plan_id-only fallback path (empty interpolated table name). ⇒ No SQLi; the leak collapses to a no-auth NAV-history enumeration + a benign error-shape oracle on a sting path with SQL Server engine disclosure. H10 downgraded to CVSS 3.9–4.3.
+- **H11 cdsl/dev route-validity oracle mapped (read-only).** Distinction: valid path+POST → `"Invalid Input Format"`; valid path+wrong method → `"Invalid request method"`; unknown resource → `"Invalid resource"`. Route matrix: `cdslEdis/{index,details,tpin}` + `edis/{index,details,tpin}` = valid POST JSON; `pledge/{userdata,status,reqdetails}` + `unpledge/status` = valid GET (then `Invalid Input Format` on wrong method); `pledge/setup` + `unpledge/setup` = POST; everything else `Invalid resource`. **No progressive field oracle** — every body (`{"client_id"}`, `{"fy_id"}`, `{"tpin"}`, JDBC/form/multipart/text) returns uniform `Invalid Input Format` (no auth fingerprint, but also no key-branching). Surface is a dev-stage EDIS/TPIN route skeleton; **no auth bypass demonstrated**, only route/method schema disclosure. H11 downgraded to CVSS 5.3 (schema disclosure only).
+- **`api/beta/*` is a dead backend** — uniform `502 Bad Gateway` on all 8 routes (get_all_plans/products/subscriptions/appThirdParty/activateThirdParty/get_msiuser_details/get_product_report) across GET/POST. No public surface; no auth oracle; deprioritized.
+- **`api-y1.fyers.in/trade/v3/{orders,orders/slice}` → 404** ("Test Page" Apache host). The config's `api-y1/...` entries do not resolve — trade-core lives on api-t2/api-t1. Confirmed 5th hostname enum (dead).
+- **New api-i1 auth-domain (fingerprint #12).** `api-i1.fyers.in/fy/mf/v1/holdings` → `{"error_code":-15,"status_code":401,"message":"Could not authenticate the user"}` — the `-15` trade-core domain, distinct from the other api-i1 domains (40101/-101/-101-variant/-100). Same hostname, per-service authz split confirmed again.
+- **`vagator/v2/guest_user/login_v2`** (api-t2) → `-1024 "please provide a valid mobile number"` (pre-auth, `-1024` new code on login path) — validation-before-auth on the guest-user login; reusable as a phone-format oracle.
+- Confirmed stable: `api-a1/funds/send_otp` 404; `api-g1/settings/user` `-401`; `quantainer/oi_stats/*` `-401`; `verif_token_v2` DRF `{"detail":Not authenticated}`; `indus/v1/config` live (now serves `ab_feature_set` + product-matrix).

+HIGH-IMPACT HYPOTHESIS IDENTIFIED (model: deepseek)
+Review research_deepseek.md for details
+
+## CVSS Candidates
+- [H10 chart SQL-engine/error oracle] — CVSS 3.9–4.1 (downgraded; NAV history is legitimately public, no SQLi proven)
+- [H11 cdsl/dev route+method schema disclosure] — CVSS 5.5 (downgraded; no auth-bypass demonstrated)
+- [H1 open redirect on login host] — CVSS 6.1 (carried)
+- [H1 OAuth code interception] — CVSS 8.1–9.0 (carried, conditional on code-delivery)

## CVSS Candidates

# 6 items on 2026-08-05 23:42:47 UTC
- **H10 closed (downgraded).** `scheme_code` is the chart table selector — `scheme_code=47941&period=1Y` returns real public NAV history; strings/nonexistent codes → empty `nav_history[]` (parameterized, **no injection**). The raw SQL-1103 error only fires on the plan_id-only/`scheme_code`-absent fallback path with an empty interpolated table name. ⇒ engine disclosure + benign error oracle, no SQLi. CVSS 3.9–4.1.
- **H11 cdsl/dev route-validity oracle mapped.** Three-way discriminator: valid path+method → `"Invalid Input Format"`; wrong method → `"Invalid request method"`; unknown resource → `"Invalid resource"`. Valid routes: `cdslEdis/{index,details,tpin}`, `edis/{index,details,tpin}` (POST JSON); `pledge/{userdata,status,reqdetails}`, `unpledge/status` (GET); `pledge/setup`/`unpledge/setup` (POST). **No progressive field oracle** — every body shape returns uniform error, no auth fingerprint. Schema disclosure only. CVSS 5.5 (downgraded).
- **`api/beta/*` dead backend** — uniform 502 across all 8 routes (GET/POST). `api-y1/trade/v3/*` → 404 (Apache Test Page host, config entries don't resolve).
- **New auth fingerprint #12:** `api-i1/fy/mf/v1/holdings` → `-15 "Could not authenticate the user"` (trade-core domain on api-i1, distinct from its 40101/-101/-100).
- **`vagator/v2/guest_user/login_v2`** → `-1024 "please provide a valid mobile number"` (validation-before-auth phone oracle, pre-auth).
- Confirmed stable: api-a1 send_otp 404, api-g1 `-401`, quantainer `-401`, `indus/v1/config` live (now with `ab_feature_set` product matrix).

HIGH-IMPACT HYPOTHESIS IDENTIFIED (model: deepseek)
Review research_deepseek.md for details
  - [H8 MCP FIA_TOKEN cross-domain scoping / tool ATO] — CVSS 6.5–8.0 (conditional)
-+- **NEW: raw Microsoft SQL Server error leak (unauthenticated) on `api-i1.fyers.in/mf/data/v2/scheme/details/chart`.** With valid `period` (1M/1Y/3M/5Y; `1D` is invalid) returns `{"Number":1103,"SQLState":[52,50,48,48,48],"Message":"Incorrect table name ''"}` — SQLState decodes to "42000". Backend is SQL Server; the chart table name is interpolated server-side (empty for unknown params), and the raw DB error is reflected with no wrapper and **no auth**. isin/plan_id/symbol params accepted but do not set the table name → table name derived server-side. Error-based oracle + stack disclosure on a public endpoint (H10, CVSS 4.3 baseline; SQLi conditional on table-name provenance).
++- **H10 closed on chart table-name provenance.** `scheme_code` is the table selector: `scheme_code=47941&period=1Y` returns full public NAV history (`nav_history[]` timestamps+NAV); `scheme_code` is **parameterized** (strings/non-exist codes → empty `nav_history[]`, no injection). The raw SQL-1103 error only surfaces on the `scheme_code`-absent / plan_id-only fallback path (empty interpolated table name). ⇒ No SQLi; the leak collapses to a no-auth NAV-history enumeration + a benign error-shape oracle on a sting path with SQL Server engine disclosure. H10 downgraded to CVSS 3.9–4.3.

# POC round 2026-08-06 01:xx UTC — H1 evidence chain completed (static, current bundle) + step-1 live re-confirm
- **Reserved-`cb` denylist extracted verbatim from CURRENT `login.min.js?v=1.3` (18 string arrays, 91-arg alias map fully resolved; 0 unresolved decoder calls).** The post-auth handler `handleVerifyPinServiceResponse` runs: `_0x2e3204=["https://fyers.in/web","https://fyers.in/web/","https://fyers.in/web/markets/home","https://trade.fyers.in","https://trade.fyers.in/"]; if(cb && DENYLIST.includes(cb)) delete cb;` — the 5-value allowlist is **exact-match** and FYERS-internal only. Any other `cb` value survives and drives navigation.
- **Post-auth navigation branch order (current build, fully deobfuscated):** `handleTokenStatusResponse`: `q=params(); if(q['redirect_uri']){location.href=q['redirect_uri'];return} else if(q['cb']){location.href=q['cb'];return} else showOtpForm()`. `handleVerifyPinServiceResponse`: after cookie-set (`_FYERS`=access_token, `refresh_token`, `_deviceId`): `if(cb){set cookies; document.location.href=cb} else if(redirect_uri && (redirect_uri contains "myaccount"|"profile")){set cookies; location.href=redirect_uri} else if(client_id && response_type && state){new LoginClient(params,_,access_token).requestApiV2Login()} else {set cookies; showAuthenticatedPage()}`. `params()` = `new URLSearchParams(location.search)` — all values from the URL query string.
- **OAuth code-delivery path fully traced (current build):** `LoginClient` ctor sets `this.redirection_url = params.redirect_uri` (attacker-controlled). `getDirectLoginData()` → POST body `{app_id, redirect_uri: redirection_url, user_id, auth, state, scope, nonce, response_type}` to `https://api.fyers.in/api/v2/direct-login` (or `/api/v3/direct-login`, `/tradingview/auth/direct-login` variants). `handleDirectLoginResponse`: `if(s=='ok') window.location.href = data.Url`. `handleAPIV2Response`: `code==308 → window.location.assign(data.Url)`. ⇒ **the server-returned `Url` is what carries the code to the attacker-supplied redirect_uri**; whether `data.Url` includes the auth_code and whether the server validates `redirect_uri` against the app registry at issuance is the only remaining unknown (requires authenticated confirmation — documented, not executed).
- **PKCE is ineffective against this primitive:** `code_challenge` is accepted as a URL param (step-1 live-confirmed this run with `code_challenge=abc123def456`) and forwarded into the direct-login body; the attacker who crafts the phished URL also controls the verifier → code redemption by the attacker is unhindered.
- **Step-1 oracle re-confirmed LIVE on the current build (1 request, spaced):** `GET api-t1.fyers.in/api/v3/generate-authcode?client_id=SOFG221ZX4-101&redirect_uri=https://evil.example.com/cb&response_type=code&state=poc-design&code_challenge=abc123def456` → **HTTP 200, 60,917 B** login page, no allowlist rejection, no URI reflection in HTML (1 `redirect_uri` hit is the inline platform-selection JS). The returned page loads the exact bundle deobfuscated above (`login.min.js?v=1.3`).
- **Inline step-1 `cb` handler is UI-only, not a security gate:** `const cb=urlParams.get('cb'); if(cb.startsWith('https://fyers.in/web')){...} else if(cb.startsWith('https://trade.fyers.in')){...}` + `shouldShowPlatform=(!cb||cb.includes('https://fyers.in/web')||cb.includes('https://trade.fyers.in'))&&!search.includes('redirect_uri')` — `startsWith`/`includes` substring checks only pre-check the web/trader radio; they do NOT constrain the post-auth `location.href=cb` destination in `login.min.js` (exact-match denylist there).
- **H1 POC chain now fully evidenced end-to-end (read-only):** (A) step-1 `generate-authcode` accepts arbitrary `redirect_uri`/`code_challenge` [live]; (B) `getQueryStringValue()` feeds `redirect_uri`/`cb` from `location.search` [static]; (C) LoginClient forwards `params.redirect_uri` into direct-login POST [static]; (D) server `Url` becomes `location.href` [static]. Open-redirect primitive = **confirmed** (CVSS 6.1). Code-interception-to-ATO = **conditional on step (D) server behavior** (CVSS 8.1–9.0), needing authenticated confirmation of the code artifact in `data.Url`.

# 7 items on 2026-08-06 01:17:10 UTC
- **H1 POC evidence chain closed (this run).** Fully deobfuscated the *current* `login.min.js?v=1.3` (18 string arrays + 91-alias map resolved; 0 unresolved calls) and extracted the exact reserved-`cb` denylist, the post-auth navigation branch order, and the complete OAuth code-delivery path.
- **Reserved-`cb` denylist (5 values, exact-match):** `https://fyers.in/web`, `https://fyers.in/web/`, `https://fyers.in/web/markets/home`, `https://trade.fyers.in`, `https://trade.fyers.in/`. Handler: `if(cb && DENYLIST.includes(cb)) delete cb`. Any other `cb`/`redirect_uri` value is honored.
- **Post-auth branch order (static):** `redirect_uri` → `cb` → `(client_id&&response_type&&state)` OAuth → default page. `params()` reads `new URLSearchParams(location.search)` — all attacker-controlled in a phished URL.
- **Code-delivery path (static):** `LoginClient.redirection_url = params.redirect_uri` → POST `/api/v2/direct-login` (body `{app_id, redirect_uri, user_id, auth, state, scope, nonce, response_type}`) → `handleDirectLoginResponse`: `location.href = data.Url`. **Server `Url` is the code carrier; whether it embeds `auth_code` + registry-validates `redirect_uri` is the sole remaining unknown.**
- **PKCE ineffective:** attacker controls `code_challenge`/verifier in the phished URL (step-1 accepts it — live-confirmed with `code_challenge=abc123def456`).
- **Step-1 re-confirmed live on current build:** `generate-authcode?client_id=SOFG221ZX4-101&redirect_uri=https://evil.example.com/cb&...&code_challenge=...` → HTTP 200, 60,917 B login page, no allowlist rejection, no URI reflection.
- **Step-1 inline `cb` handler is UI-only** (`startsWith` radio pre-check), not a security gate — does not constrain the post-auth `location.href=cb`.

HIGH-IMPACT HYPOTHESIS IDENTIFIED (model: deepseek)
Review research_deepseek.md for details
[ ] Construct H1 POC design doc (exact curl sequence, success indicators, CVSS) and append to findings
[•] Construct H1 POC design doc (exact curl sequence, success indicators, CVSS) and append to findings
   - [H8 MCP FIA_TOKEN cross-domain scoping / tool ATO] — CVSS 6.5–8.0 (conditional)
