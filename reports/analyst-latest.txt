# Target: fyers.in
# Current Phase: HYPOTHESIS

## Key Findings
- **H22 finalized (api-i1 eIPO object-keyed IDOR, conditional):** full object-key inventory is caller-controlled and none demonstrably session-bound — `OrderId`+`order_id`, `IssueId`/`IPOIssueId`, `FormFromId`/`FormToId` (application-form ranges), `IssueSeries`, `offer_id`, `RequestedBy`, plus PII place-order fields (`UPIID`, `BankAccountNo`, `DPIdBeneficiary`). Backend KNOWS the owner (`/invest/investors-details` returns session `client_id`/`client_name`) → owner check is feasible; absence unproven pre-auth. FYERS-side two-account validation only.
- **14-field /invest order-book schema enumerated (validation-before-auth):** `pageNumber, OrderId, order_id, IssueId, FormFromId, FormToId, FromDate, ToDate, OrderStatus, OrderCategory, IssueSeries, RequestedBy, EncryptReportResponse, ResponseType` (single Pydantic model; partial input shrinks the missing list). `EncryptReportResponse`/`ResponseType` → report-generation mode behind auth (FYERS-side confirm).
- **tapi/eipo auth-first INVARIANCE confirmed (read-only):** full 36-field InitialObj on `/eipo/place-order` and full 14-field body on `/eipo/order-book` both → `-100` pre-schema ⇒ no pre-auth oracle/order primitive on tapi.
- **Negatives:** no pre-auth data reachable on any api-i1 route; jhelum offer feed remains public-by-design informational.

## API Endpoints (this run)
- api-i1.fyers.in /invest/v1/ipo/order-book (POST, token; 14-field schema oracle pre-auth; keying: OrderId/order_id/IssueId/FormFromId/FormToId/IssueSeries)
- api-i1.fyers.in /investment/tapi/v1/eipo/{place-order,order-book} (auth-first -100 invariant)
- api-i1.fyers.in /investment/tapi/v1/eipo/cancel-order (DELETE ?order_id=&offer_id=; auth-first -100)

## Hypotheses
### H22 — eIPO order/application object-keyed IDOR (finalized; A7/E7/B9 in package)
Reasoning: all object keys caller-supplied, backend knows session owner, scoping unproven pre-auth (both /invest and tapi auth-gate tokenless input).
Impact: cross-account order/application read (Medium, CVSS 5.3–6.5) · cross-account order CANCEL (state-modifying, CVSS 6.5–7.5). Conditional.
Test: own-session baseline → foreign OrderId/order_id/FormFromId/FormToId/IssueSeries (read) and foreign order_id on cancel-order (write); FYERS-side two-account validation, NOT executed.

## CVSS Candidates
- [H17 req_id] 8.1–9.1 · [H19 unauth upload/SSRF] 5.3–7.5 · [H15/H18] 5.3–7.5 · [H13/H21] 5.3–6.5 · [H22] 5.3–7.5 conditional (read 5.3–6.5, cancel 6.5–7.5)
- [B1–B9 informational bundle incl. eIPO 14-field schema oracle, OAuth state/client, public offer feed] — informational/low

---
## STATE UPDATE FOR NEXT RUN
STATUS_TARGET: fyers.in
STATUS_PHASE: HYPOTHESIS
STATUS_STATE: IN_PROGRESS

NEXT_STEP_1: HYPOTHESIS on api-i1 eIPO keying COMPLETE — advance to POC: write exact 14-field order-book body + foreign-delta substitution steps (own-session baseline → foreign OrderId/order_id/FormFromId/FormToId) into E7/A7; reconcile H22 severity 5.3–7.5 conditional across package.
NEXT_STEP_2: Operator extends scope-confirm with C9 (api-i1.fyers.in / ipo.fyers.in, absent from both public configs) + submits package (A1–A7 + B1–B9 + C + D + E1–E7 + F) via forms.fyers.in BugBountyForm1; no pre-auth order payloads, no OTP, no cross-account.
NEXT_STEP_3: Exclusions unchanged (SmartHunt SSTI false positive 4×, third-party hosts, fydev.tech/fyers.co.in dev-staging twins, public config artifacts, api-t1 CF bot-block noise); guardrails hold (spaced read-only probes only).
